// #include <regex>
// #include <iostream>
// #include <string>

// bool validateToken(const std::string& token) {
//     std::regex pattern("^[a-zA-Z][0-9]+");
//     return std::regex_match(token, pattern);
// }

// int main() {
//     std::string token1 = "a1";
//     std::string token2 = "A9";
//     std::string token3 = "c12";
//     std::string token4 = "dot";

//     std::cout << token1 << ": " << (validateToken(token1) ? "Valid" : "Invalid") << std::endl;
//     std::cout << token2 << ": " << (validateToken(token2) ? "Valid" : "Invalid") << std::endl;
//     std::cout << token3 << ": " << (validateToken(token3) ? "Valid" : "Invalid") << std::endl;
//     std::cout << token4 << ": " << (validateToken(token4) ? "Valid" : "Invalid") << std::endl;

//     return 0;
// }
